#syntax=docker/dockerfile

FROM registry.gitlab.com/uniget-org/images/ubuntu:26.04 AS prepare
COPY --from=ghcr.io/uniget-org/tools/uniget-build:latest \
    /etc/profile.d/ \
    /etc/profile.d/
SHELL [ "bash", "-clo", "errexit" ]
ARG name
ARG version
RUN --mount=type=cache,target=/var/cache/uniget/download <<EOF
url="https://github.com/crossplane-contrib/crossplane-diff/releases/download/v${version}/crossplane-diff_linux_${alt_arch}.tar.gz"
filename="$( basename "${url}" )"

check-github-release-asset "crossplane-contrib/crossplane-diff" "v${version}" "${filename}"
curl --silent --show-error --location --fail --output "${uniget_cache_download}/${filename}" \
    "${url}"

tar --file "${uniget_cache_download}/${filename}" --list
tar --file "${uniget_cache_download}/${filename}" --extract --directory "${prefix}/bin/" --no-same-owner \
    crossplane-diff
EOF
