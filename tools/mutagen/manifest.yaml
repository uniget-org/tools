# yaml-language-server: $schema=https://tools.uniget.dev/schema.yaml
name: mutagen
version: "0.18.1"
check: ${binary} version
tags:
- component/network
- component/storage
- category/development
- type/cli
- type/daemon
- lang/go
homepage: https://mutagen.io
repository: https://github.com/mutagen-io/mutagen
description: Fast file synchronization and network forwarding for remote development
renovate:
  datasource: github-releases
  package: mutagen-io/mutagen
  extractVersion: ^v(?<version>.+)$
  priority: low
license:
  name: Custom
  link: https://github.com/mutagen-io/mutagen/blob/master/LICENSE
