# yaml-language-server: $schema=https://tools.uniget.dev/schema.yaml
$schema: https://tools.uniget.dev/schema.yaml
name: compose-bridge
version: "0.0.20"
check: "" # No version parameter
platforms:
- linux/amd64
- linux/arm64
tags:
- org/docker
- category/development
- lang/go
- type/cli
- standard/compose
- kubernetes
- k8s
- templating
- state/onemanshow
- state/deprecated
homepage: https://github.com/docker/compose-bridge-binaries
repository: https://github.com/docker/compose-bridge-binaries
description: Reuse compose.yaml but translate it into another platform's definition format, with a primary focus on Kubernetes
messages:
  internals: |
    See the official documentation at https://docs.docker.com/compose/bridge/
renovate:
  datasource: github-releases
  package: docker/compose-bridge-binaries
  extractVersion: ^v(?<version>.+)$
  priority: low
license:
  name: Docker Subscription Service Agreement
  link: https://www.docker.com/legal/docker-subscription-service-agreement
