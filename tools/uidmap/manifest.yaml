# yaml-language-server: $schema=https://tools.uniget.dev/schema.yaml
name: uidmap
description: Programs to help use subuids
license:
  name: BSD 3-Clause License
  link: https://github.com/shadow-maint/shadow/blob/master/COPYING
homepage: https://github.com/shadow-maint/shadow
repository: https://github.com/shadow-maint/shadow
version: "4.11.1"
tags:
- kernel/pid
- kernel/namespaces
- type/cli
- lang/c
binary: newuidmap
check: "" # No version parameter
build_dependencies:
- make
platforms:
- linux/amd64
messages:
  usage: |
    Make sure that newuidmap and newgidmap have the setuid bit set.
renovate:
  datasource: github-releases
  package: shadow-maint/shadow
  extractVersion: ^v(?<version>.+)$
