# yaml-language-server: $schema=https://tools.uniget.dev/schema.yaml
name: gitsign
version: "0.10.2"
check: ${binary} --version | grep ^gitsign | cut -d' ' -f3 | tr -d v
build_dependencies:
- cosign
runtime_dependencies:
- wslu
- xdg-utils
platforms:
- linux/amd64
- linux/arm64
tags:
- category/development
- category/security
- type/cli
- lang/go
- standard/git
- git
homepage: https://github.com/sigstore/gitsign
description: Keyless Git signing using Sigstore
messages:
  internals: |
    - Copy gitsign-credential-cache.sh from TARGET/etc/profile.d to /etc/profile.d/ or ~/.local/etc/profile.d/
    - Copy systemd units from TARGET/etc/systemd/user/ to /etc/systemd/user/ or ~/.config/systemd/user/
    - Enable both systemd units and start the socket unit
renovate:
  datasource: github-releases
  package: sigstore/gitsign
  extractVersion: ^v(?<version>.+?)$
  priority: high
