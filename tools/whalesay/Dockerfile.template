#syntax=docker/dockerfile:1.6.0

ARG ref=main

FROM ghcr.io/uniget-org/tools/base:${ref} AS prepare
ARG name
ARG version
COPY docker.cow ${prefix}${target}/share/cows/
RUN <<EOF
ln --symbolic --relative --force "${prefix}${target}/bin/cowsay" "${prefix}${target}/bin/whalesay"
EOF