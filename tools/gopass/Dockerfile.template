#syntax=docker/dockerfile:1.8.0

FROM ghcr.io/uniget-org/images/ubuntu:24.04@sha256:6ff62e5ed6b1c33f0ae1644692a354281534a37ec4d752cf480850e19a94d16d AS prepare
COPY --from=ghcr.io/uniget-org/tools/uniget-build:latest \
    /etc/profile.d/ \
    /etc/profile.d/
SHELL [ "bash", "-clo", "errexit" ]
ARG name
ARG version
RUN <<EOF
check-github-release-asset "gopasspw/gopass" "v${version}" "gopass-${version}-linux-${alt_arch}.tar.gz"
curl --silent --show-error --location --fail "https://github.com/gopasspw/gopass/releases/download/v${version}/gopass-${version}-linux-${alt_arch}.tar.gz" \
| tar --extract --gzip --directory="${prefix}/bin/" --no-same-owner \
    gopass

curl --silent --show-error --location --fail --output "${prefix}/share/man/man1/gopass.1" \
    "https://github.com/gopasspw/gopass/raw/v${version}/gopass.1"

"${prefix}/bin/gopass" completion bash >"${prefix}/share/bash-completion/completions/gopass"
"${prefix}/bin/gopass" completion fish >"${prefix}/share/fish/vendor_completions.d/gopass.fish"
"${prefix}/bin/gopass" completion zsh >"${prefix}/share/zsh/vendor-completions/_gopass"
EOF