.docker_login:
  before_script: |
    echo "${CI_REGISTRY_PASSWORD}" \
    | docker login "${CI_REGISTRY}" \
        --username="${CI_REGISTRY_USER}" \
        --password-stdin

.docker_logout:
  after_script: |
    docker logout "${CI_REGISTRY}"

.dind:
  variables:
    DOCKER_CERT_DIR: ""
  services:
  - name: docker:29.2.0-dind@sha256:6a58fe0da3d9c36bae9adb37ec6af8cb07b5a1d02aa72ae5ad370d7ecbafdfcd

.uniget:
  before_script: |
    apk add --update-cache \
        curl
  script: |
    curl -sLf https://gitlab.com/uniget-org/cli/-/releases/permalink/latest/downloads/uniget_Linux_$(uname -m).tar.gz \
    | tar -xzC /usr/local/bin uniget

collect:
  rules:
  - if: $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  image: alpine:3.23.2@sha256:865b95f46d98cf867a156fe4a135ad3fe50d2056aa3f25ed31662dff6da4eb62
  before_script:
  - |
    apk add --update-cache \
        curl \
        git
  - !reference [.uniget, script]
  - uniget install jq regclient
  script:
  - |
    type regctl
    type jq
    type git
  - |
    METADATA_SHA="$(
        regctl manifest get registry.gitlab.com/uniget-org/tools/metadata:main \
            --platform=linux/amd64 \
            --format=raw-body \
        | jq --raw-output '.layers[0].digest' \
        | xargs -I{} \
            regctl blob get registry.gitlab.com/uniget-org/tools/metadata:main {} \
        | tar --extract --gzip --to-stdout \
        | jq --raw-output '.revision'
    )"
    echo "### Fetching changed tools for ${METADATA_SHA}..${CI_COMMIT_SHA}"
  - |
    CHANGED_TOOLS="$(
        git log --pretty=format: --name-only --diff-filter=d ${METADATA_SHA}..${CI_COMMIT_SHA} \
        | sort \
        | grep "^tools/" \
        | cut -d/ -f2 \
        | uniq \
        | while read -r TOOL; do test -d "tools/${TOOL}" && echo "${TOOL}"; done \
        | xargs
    )"
    echo "### Tools to process: ${CHANGED_TOOLS}."
  - |
    CHANGED_TOOLS_JSON="$(
        echo -n "${CHANGED_TOOLS}" \
        | tr ' ' '\n' \
        | jq --raw-input '.' \
        | jq --slurp --compact-output '.'
    )"
    echo "    JSON: ${CHANGED_TOOLS_JSON}"

metadata:
  rules:
  - if: $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  needs:
  - collect
  extends:
  - .docker_login
  - .dind
  - .docker_logout
  image: docker:29.2.0@sha256:eb37f58646a901dc7727cf448cae36daaefaba79de33b5058dab79aa4c04aefb
  script:
  - apk add --update-cache \
        curl \
        make \
        bash
  - !reference [.uniget, script]
  - make metadata.json--push REGISTRY=${CI_REGISTRY}
